<h1>タスク一覧</h1>
<table>
  <thead>
    <tr>
      <th>タスク名</th>
      <th>詳細</th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.title %></td>
        <td><%= task.content %></td>
        <td></td>
      </tr>
    <% end %>
    <tr>
    </tr>
  </tbody>
</table>

<%= link_to "タスクを作成する", new_task_path %>